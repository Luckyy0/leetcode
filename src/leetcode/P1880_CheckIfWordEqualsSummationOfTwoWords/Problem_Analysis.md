# Problem Analysis: Check if Word Equals Summation of Two Words

## Problem Description
`first`, `second`, `target` words.
Check if `val(first) + val(second) == val(target)`.
`val(s)`: 'a'->0, 'b'->1... 'j'->9. Concatenate.

## Analysis

1.  **Parsing**:
    -   Helper function string to int.
    -   Sum and compare.

## Implementation Details
-   Loop.
