# Problem Analysis: Find Followers Count

## Problem Description
User `user_id` follows `follower_id`? Or other way?
Usually `Followers` (user_id, follower_id). `follower_id` follows `user_id`?
Or `(user_id, follower_id)` means user `user_id` has follower `follower_id`.
Return `user_id` and count.

## Analysis

1.  **Count**:
    -   `GROUP BY` user_id.

## Implementation Details
-   SQL.
