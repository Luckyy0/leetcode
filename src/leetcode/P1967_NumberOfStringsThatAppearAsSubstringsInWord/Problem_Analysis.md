# Problem Analysis: Number of Strings That Appear as Substrings in Word

## Problem Description
Array `patterns`. String `word`.
Count patterns that are substrings of `word`.

## Analysis

1.  **Iterate and Check**:
    -   `match` count.
    -   `word.contains(p)`.

## Implementation Details
-   String `contains`.
