# Problem Analysis: Check Whether Two Strings are Almost Equivalent

## Problem Description
`word1`, `word2` same length.
Almost equivalent if for every char 'a'-'z', `abs(freq1[acc] - freq2[acc]) <= 3`.

## Analysis

1.  **Frequency Count**:
    -   Count chars in both.
    -   Loop 0 to 25. Check diff.

## Implementation Details
-   2 arrays size 26.
