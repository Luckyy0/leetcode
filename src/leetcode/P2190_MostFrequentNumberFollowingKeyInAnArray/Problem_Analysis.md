# Problem Analysis: Most Frequent Number Following Key In an Array

## Problem Description
Array `nums`, integer `key`.
Count occurrences of `target` such that `nums[i] == key` and `nums[i+1] == target`.
Return target with max count.

## Analysis

1.  **Counting**:
    -   Map or array counts.
    -   Iterate `0` to `n-2`.

## Implementation Details
-   Loop.
